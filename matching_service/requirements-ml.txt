# Heavyweight ML dependencies for semantic ranking.
# Install on top of requirements.txt when GPU / semantic search is needed:
#   pip install -r requirements.txt -r requirements-ml.txt
sentence-transformers>=3.0.0
